{
    "name": "kerimdemirkaynak/demo-project",
    "description": "Example PHP project with Composer validation",
    "type": "project",
    "require": {
        "php": "^8.1",
        "monolog/monolog": "^3.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "scripts": {
        "test": "phpunit --testdox"
    }
}
